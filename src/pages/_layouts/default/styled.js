import styled from 'styled-components'

import { colors } from '~/themes'

export const Wrapper = styled.div`
  height: 100%;
  /* background: linear-gradient(-90deg, ${colors.PRIMARY}, ${colors.SECONDARY} ) */
  background: ${colors.GREY_THREE};
`
